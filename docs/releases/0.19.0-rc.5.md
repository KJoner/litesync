**0.19.0 第五个预发布**：iOS 密码管理器填充连坐的最终修复。
配套服务端 v0.19.0-rc.3。协议 v6 不变。

## 修复（iOS 真机实测）

- **重命名结果不再依赖自动填充行为**：Obsidian 弹窗叠在同一个网页文档里，
  密码管理器填充会把条目「用户名」塞进下层的名字框——曾导致仓库被改名成
  密码条目名。现在：被拒那一刻捕获你输入的名字，Token 确定后的自动重试
  **只用捕获值**；Token 弹窗期间名字框只读（填充跳过只读控件）、关闭时
  恢复捕获值。向导新建仓库名同款防护。

其余与 0.19.0-rc.4 相同。
