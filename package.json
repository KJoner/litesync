{
  "name": "litesync",
  "version": "0.7.1",
  "description": "Sync your vault with your own self-hosted LiteSync server.",
  "main": "main.js",
  "scripts": {
    "dev": "node esbuild.config.mjs",
    "build": "tsc -noEmit -skipLibCheck && node esbuild.config.mjs production",
    "test": "esbuild tests/unit.test.ts tests/merge.test.ts tests/crypto.test.ts tests/device-trust.test.ts tests/store.test.ts --bundle --platform=node --format=cjs --outdir=.test-build && node --test .test-build/unit.test.js .test-build/merge.test.js .test-build/crypto.test.js .test-build/device-trust.test.js .test-build/store.test.js",
    "test:mobile": "node scripts/check-mobile.mjs && npm run build && npm test"
  },
  "keywords": [
    "obsidian",
    "sync"
  ],
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^22.10.0",
    "esbuild": "^0.25.0",
    "obsidian": "^1.13.1",
    "tslib": "^2.8.0",
    "typescript": "^5.7.0"
  }
}
